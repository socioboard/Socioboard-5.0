server {
    listen ${HTTP_PORT};
    server_name *.${BASE_DOMAIN};
    server_tokens off;

    location ^~ / {
        return 301 https://$host$request_uri;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ /admin {
        proxy_pass http://socioboard:8080/admin;
    }

    location ^~ / {
        proxy_pass http://socioboard:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ~ \.css {
        add_header  Content-Type    text/css;
    }
    location ~ \.js {
        add_header  Content-Type    application/x-javascript;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio-api.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ / {
        proxy_pass http://socioboard:3000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio-feeds.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ / {
        proxy_pass http://socioboard:3001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio-publish.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ / {
        proxy_pass http://socioboard:3002/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio-update.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ / {
        proxy_pass http://socioboard:3003/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
server {
    listen              ${HTTPS_PORT} ssl;
    server_name         socio-notifications.${BASE_DOMAIN};
    keepalive_timeout   70;

    access_log          /dev/stdout;
    error_log           /dev/stdout;

    add_header X-Frame-Options    "SAMEORIGIN";
    add_header X-XSS-Protection   "1; mode=block";
    server_tokens                 off;

    ssl_certificate /ssl/cert.pem;
    ssl_certificate_key /ssl/key.pem;


    location ^~ / {
        proxy_pass http://socioboard:3004/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
